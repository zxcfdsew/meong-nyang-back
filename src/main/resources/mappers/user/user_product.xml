<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meongnyang.shop.repository.user.UserProductMapper">
    <select id="findProducts" resultType="com.meongnyang.shop.entity.Product">
        select
            id,
            product_name as productName,
            pet_group_id as petGroupId,
            category_id as categoryId,
            product_price as productPrice,
            product_price_discount as productPriceDiscount,
            product_detail as productDetail,
            product_brand as productBrand,
            product_model as productModel
        from
            product_tb
    </select>
    <select id="findProductCount" resultType="java.lang.Integer">
        select
            count(*)
        from
            product_tb
    </select>
</mapper>